A variable is a placeholder and it stores in a memory. In golang we need to declare a variable before using it.
We can declare a variable in go in multiple ways

```golang

var variablename dataType
```